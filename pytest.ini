[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Testes unitários de funções isoladas
    integration: Testes de integração do pipeline completo
    slow: Testes que demoram mais (ex: pipeline completo)
